---
import { siteConfig } from "@/config/site";
import { withBase } from "@/lib/urls";

const target = withBase("/ueber-mich/");
const robots = siteConfig.previewMode ? "noindex,nofollow,noarchive" : "noindex,follow";
---

<!doctype html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content={robots} />
    <meta name="description" content="Weiterleitung zur persönlichen Seite von Denys Rybinskyi." />
    <meta http-equiv="refresh" content={`0;url=${target}`} />
    <link rel="canonical" href={`${siteConfig.productionUrl}/ueber-mich/`} />
    <title>Weiterleitung zu Über mich | Rybinskyi BauXpert</title>
  </head>
  <body>
    <p>Die Seite heißt jetzt <a href={target}>Über mich</a>.</p>
  </body>
</html>
